{
  "version": 2,
  "buildCommand": "cd UI_Logs && npm install && npm run build",
  "outputDirectory": "UI_Logs/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}